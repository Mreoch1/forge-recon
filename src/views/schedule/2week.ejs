<%- include('../layouts/header', { title: title, currentUser: currentUser, flash: flash, activeNav: activeNav }) %>
<style>
  .sc-layout { display: flex; flex-direction: column; height: calc(100vh - 3.5rem); }
  .sc-header { display: flex; align-items: center; gap: .75rem; padding: .75rem 1.25rem; border-bottom: 1px solid #e0e0e0; flex-shrink: 0; flex-wrap: wrap; }
  .sc-header h1 { font-size: 1rem; font-weight: 600; margin: 0; }
  .sc-header .sc-date-range { font-size: .82rem; color: #666; }
  .sc-nav-btn { padding: .3rem .7rem; border: 1px solid #d0d0d0; border-radius: 4px; background: #fff; font-size: .78rem; cursor: pointer; }
  .sc-nav-btn:hover { background: #f5f5f5; }
  .sc-today-btn { padding: .3rem .7rem; border: 1px solid #1a1a1a; border-radius: 4px; background: #1a1a1a; color: #fff; font-size: .78rem; cursor: pointer; }
  .sc-today-btn:hover { background: #333; }
  .sc-header select { font-size: .78rem; padding: .25rem .5rem; border: 1px solid #d0d0d0; border-radius: 4px; }
  .sc-view-toggle { display: flex; gap: 2px; }
  .sc-view-toggle a { padding: .25rem .6rem; border: 1px solid #d0d0d0; border-radius: 4px; font-size: .72rem; color: #555; text-decoration: none; }
  .sc-view-toggle a:hover { background: #f0f0f0; }
  .sc-view-toggle a.active { background: #c0202b; color: #fff; border-color: #c0202b; }

  .sc-grid-wrap { flex: 1; overflow-y: auto; }
  .sc-week-row { display: flex; }
  .sc-grid { display: grid; grid-template-columns: 3.5rem repeat(7, 1fr); min-height: 840px; flex-shrink: 0; }
  .sc-week-label { display: flex; align-items: center; justify-content: center; padding: .4rem 0; font-size: .75rem; font-weight: 600; color: #555; background: #fafafa; border-bottom: 2px solid #ddd; }
  .sc-hour-gutter { grid-column: 1; border-right: 1px solid #e8e8e8; position: relative; }
  .sc-hour-label { position: absolute; right: .4rem; font-size: .6rem; color: #999; font-family: ui-monospace,monospace; transform: translateY(-50%); white-space: nowrap; }
  .sc-day-col { position: relative; border-right: 1px solid #f0f0f0; }
  .sc-day-col.is-today { background: #fafbfe; }
  .sc-day-col:last-child { border-right: none; }
  .sc-day-header { text-align: center; padding: .3rem 0; font-size: .65rem; font-weight: 600; color: #555; border-bottom: 1px solid #e0e0e0; position: sticky; top: 0; background: #fff; z-index: 2; }
  .sc-day-header .sc-day-num { display: block; font-size: .9rem; font-weight: 700; color: #1a1a1a; }
  .sc-day-header.is-today { background: #f0f4ff; }
  .sc-day-header.is-today .sc-day-num { color: #2563eb; }
  .sc-hour-line { position: absolute; left: 0; right: 0; border-top: 1px solid #f0f0f0; pointer-events: none; }
  .sc-now-line { position: absolute; left: 0; right: 0; border-top: 1px solid #ef4444; z-index: 3; pointer-events: none; }
  .sc-now-dot { position: absolute; left: -3px; top: -3px; width: 7px; height: 7px; border-radius: 50%; background: #ef4444; }
  .sc-wo-block { position: absolute; left: 2px; right: 2px; border-radius: 3px; padding: .15rem .3rem; font-size: .58rem; line-height: 1.2; overflow: hidden; cursor: pointer; z-index: 1; border: 1px solid rgba(0,0,0,.08); }
  .sc-wo-block:hover { box-shadow: 0 2px 6px rgba(0,0,0,.15); z-index: 4; }
  .sc-wo-block .sc-wo-num { font-family: ui-monospace,monospace; font-size: .52rem; opacity: .8; }
  .sc-wo-block .sc-wo-customer { font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; font-size: .55rem; }
  .sc-wo-block.sc-unassigned { background: #f5f5f5 !important; border-color: #ddd; background-image: repeating-linear-gradient(45deg,transparent,transparent 4px,rgba(0,0,0,.03) 4px,rgba(0,0,0,.03) 8px) !important; }
  .sc-conflict-badge { position: absolute; top: 1px; right: 1px; font-size: .55rem; z-index: 5; cursor: help; }
  .sc-conflict-tip { display: none; position: absolute; top: 0; right: 110%; background: #1a1a1a; color: #fff; font-size: .58rem; padding: .25rem .4rem; border-radius: 4px; white-space: nowrap; z-index: 10; pointer-events: none; }
  .sc-conflict-badge:hover .sc-conflict-tip { display: block; }
  .sc-empty { text-align: center; padding: 3rem 1rem; color: #999; }

  /* Confirm card */
  .sc-confirm-card { background: #fff; border: 2px dashed #c0202b; border-radius: 8px; padding: .75rem; width: 280px; z-index: 2000; box-shadow: 0 4px 16px rgba(0,0,0,.15); font-size: .78rem; line-height: 1.5; }
  .sc-confirm-card .scc-header { font-weight: 700; margin-bottom: .4rem; color: #1a1a1a; }
  .sc-confirm-card .scc-row { margin-bottom: .15rem; color: #444; }
  .sc-confirm-card .scc-label { color: #888; display: inline-block; width: 2.8rem; }
  .sc-confirm-card .scc-conflicts { margin: .4rem 0; padding: .35rem; background: #fef3c7; border-radius: 4px; font-size: .7rem; color: #92400e; }
  .sc-confirm-card .scc-conflict { margin-bottom: .1rem; }
  .sc-confirm-card .scc-actions { display: flex; gap: .5rem; margin-top: .5rem; }
  .sc-confirm-card .scc-confirm { padding: .35rem 1rem; background: #c0202b; color: #fff; border: none; border-radius: 4px; font-size: .78rem; cursor: pointer; }
  .sc-confirm-card .scc-cancel { padding: .35rem 1rem; background: transparent; border: 1px solid #ccc; border-radius: 4px; font-size: .78rem; cursor: pointer; }
  .sc-confirm-card .scc-cancel:hover { background: #f5f5f5; }
</style>

<div class="sc-layout">
  <!-- Header -->
  <div class="sc-header">
    <a href="/schedule?view=2week&date=<%= prevDate %><%= assigneeFilter ? '&assignee=' + assigneeFilter : '' %>" class="sc-nav-btn">&larr;</a>
    <a href="/schedule?view=2week&date=<%= nextDate %><%= assigneeFilter ? '&assignee=' + assigneeFilter : '' %>" class="sc-nav-btn">Next &rarr;</a>
    <a href="/schedule?view=2week" class="sc-today-btn">Today</a>
    <h1>Schedule</h1>
    <span class="sc-date-range"><%= fmtDate(weekStart) %> – <%= fmtDate(weekEnd) %></span>

    <div class="sc-view-toggle">
      <a href="/schedule?view=week&date=<%= rawDate %><%= assigneeFilter ? '&assignee=' + assigneeFilter : '' %>">Week</a>
      <a href="/schedule?view=2week&date=<%= rawDate %><%= assigneeFilter ? '&assignee=' + assigneeFilter : '' %>" class="active">2 weeks</a>
      <a href="/schedule?view=month&date=<%= rawDate %><%= assigneeFilter ? '&assignee=' + assigneeFilter : '' %>">Month</a>
    </div>

    <div style="flex:1"></div>
    <form method="get" action="/schedule" class="flex items-center gap-2">
      <input type="hidden" name="view" value="2week">
      <input type="hidden" name="date" value="<%= weekStart %>">
      <select name="assignee" onchange="this.form.submit()">
        <option value="">All</option>
        <% (users || []).forEach(function(u){ %>
          <option value="<%= u.id %>" <%= Number(assigneeFilter) === u.id ? 'selected' : '' %>><%= u.name %></option>
        <% }); %>
      </select>
      <a href="/work-orders/new" class="sc-nav-btn" style="font-weight:600;color:#1a1a1a;">+ New WO</a>
    </form>
  </div>

  <!-- Grid body: two 7-column weeks stacked -->
  <div class="sc-grid-wrap">
    <%
    var week1Days = days.slice(0, 7);
    var week2Days = days.slice(7, 14);
    function renderWeek(weekDays) {
    %>
    <div class="sc-week-label"><%= fmtDate(weekDays[0].date) %> – <%= fmtDate(weekDays[6].date) %></div>
    <div class="sc-grid">
      <!-- Gutter -->
      <div class="sc-hour-gutter">
        <% hours.forEach(function(h){ %>
          <div class="sc-hour-label" style="top:<%= ((h - HOURS_START) / HOUR_COUNT * 100) %>%"><%= h %></div>
          <div class="sc-hour-line" style="top:<%= ((h - HOURS_START) / HOUR_COUNT * 100) %>%"></div>
        <% }); %>
      </div>
      <% weekDays.forEach(function(day){ %>
        <div class="sc-day-col<%= day.isToday ? ' is-today' : '' %>" data-day-date="<%= day.date %>">
          <div class="sc-day-header<%= day.isToday ? ' is-today' : '' %>">
            <%= day.label %> <span class="sc-day-num"><%= day.num %></span>
          </div>
          <% hours.forEach(function(h){ %>
            <div class="sc-hour-line" style="top:<%= ((h - HOURS_START) / HOUR_COUNT * 100) %>%"></div>
          <% }); %>
          <% if (day.isToday && nowOffset !== null) { %>
            <div class="sc-now-line" style="top:<%= nowOffset %>%"><div class="sc-now-dot"></div></div>
          <% } %>
          <%
          var dayWos = wos.filter(function(w){ return w.scheduled_date === day.date; });
          dayWos.forEach(function(wo){
            var parts = (wo.scheduled_time||'08:00').split(':');
            var startMin = (parseInt(parts[0],10)-HOURS_START)*60+parseInt(parts[1],10);
            var topPct = (startMin/TOTAL_MINUTES)*100;
            var dh = parseInt(process.env.WO_DEFAULT_DURATION_HOURS||'4',10);
            var hPct = Math.max(dh*60/TOTAL_MINUTES*100, 3);
            var color = colorForUser(wo.assigned_to_user_id);
            var isUnassigned = !wo.assigned_to_user_id && !wo.assigned_to;
            var hasConflict = !!woConflicts[wo.id];
          %>
            <a href="/work-orders/<%= wo.id %>" class="sc-wo-block<%= isUnassigned ? ' sc-unassigned' : '' %>"
               style="top:<%= topPct.toFixed(1) %>%<%= color ? ';background:'+color : '' %>;height:<%= hPct.toFixed(1) %>%"
               data-wo-id="<%= wo.id %>" data-current-date="<%= wo.scheduled_date %>" data-current-time="<%= wo.scheduled_time||'' %>"
               data-wo-display="<%= wo.display_number %>" data-assignee-id="<%= wo.assigned_to_user_id||'' %>">
              <% if (hasConflict) { %>
                <span class="sc-conflict-badge">
                  &#9888;<span class="sc-conflict-tip"><% woConflicts[wo.id].forEach(function(c){ %>WO-<%= c.display_number %>: <%= c.overlap_minutes %>min<br><% }); %></span>
                </span>
              <% } %>
              <div class="sc-wo-num">WO-<%= wo.display_number %></div>
              <div class="sc-wo-customer"><%= (wo.customer_name||'').slice(0,25) %></div>
              <div class="sc-wo-assignee"><%= wo.assigned_user_name||wo.assigned_to||'' %></div>
            </a>
          <% }); %>
        </div>
      <% }); %>
    </div>
    <% } %>

    <% if (week1Days.length === 7) { %><% renderWeek(week1Days) %><% } %>
    <% if (week2Days.length > 0) { %><% renderWeek(week2Days) %><% } %>

    <% if (wos.length === 0) { %>
      <div class="sc-empty">No work scheduled this period.</div>
    <% } %>
  </div>
</div>
<script src="/js/schedule-drag.js" defer></script>
<%- include('../layouts/footer') %>
