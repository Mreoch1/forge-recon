<%- include('../layouts/header', { title: 'Reset password', currentUser: null, flash: typeof flash !== 'undefined' ? flash : {} }) %>

<div class="min-h-[80vh] flex items-center justify-center px-4">
  <div class="card w-full max-w-md p-8">
    <div class="flex flex-col items-center mb-6">
      <img src="/logos/recon.png" alt="Recon Construction" class="h-16 w-auto mb-2">
      <span class="forge-wordmark" style="font-size:1.5rem;font-weight:800;letter-spacing:.16em;background:linear-gradient(100deg,#4a4a4a 0%,#8a8a8a 20%,#cfcfcf 35%,#ffffff 48%,#cfcfcf 60%,#8a8a8a 75%,#4a4a4a 100%);background-size:250% 100%;background-position:100% center;background-clip:text;-webkit-background-clip:text;-webkit-text-fill-color:transparent;color:transparent;text-shadow:0 1px 0 rgba(0,0,0,0.15),0 2px 4px rgba(0,0,0,0.08);animation:forge-shine 3.5s linear infinite;">FORGE</span>
      <p class="text-sm text-recon-fog mt-1">Choose a new password</p>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="mb-4 p-3 rounded bg-red-50 border border-red-200 text-red-800 text-sm"><%- error %></div>
    <% } %>

    <form action="/reset-password/<%= token %>" method="post" class="space-y-4">
      <input type="hidden" name="token" value="<%= token %>">

      <%- include('../partials/password-validator', { fieldId: 'new_password', label: 'New password', confirmFieldId: 'confirm_password' }) %>

      <div>
        <label for="confirm_password" class="label">Confirm password</label>
        <input type="password" name="confirm_password" id="confirm_password" class="input" required autocomplete="new-password">
      </div>

      <button type="submit" class="btn btn-primary w-full justify-center">
        Update password
      </button>
    </form>

    <p class="text-center text-xs text-recon-fog mt-4">
      <a href="/login" class="text-recon-red hover:underline">&larr; Back to sign in</a>
    </p>
  </div>
</div>

<%- include('../layouts/footer') %>