<%- include('../layouts/header', { title: title, currentUser: currentUser, flash: flash, activeNav: activeNav }) %>

<div class="document-edit-shell">
  <div class="mb-4">
    <a href="/estimates/<%= estimate.id %>" class="text-sm text-recon-fog hover:text-recon-red">&larr; <%= estimate.display_number %></a>
  </div>
  <h1 class="page-h1 mb-6">Edit <%= estimate.display_number %></h1>

  <%- include('_form', {
    estimate: estimate, errors: errors, units: units,
    formAction: '/estimates/' + estimate.id, submitLabel: 'Save changes'
  }) %>
</div>

<%- include('../layouts/footer') %>
